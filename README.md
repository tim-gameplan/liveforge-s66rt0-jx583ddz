# An infinite canvas where users can type anywhere to conve…

An infinite canvas where users can type anywhere to converse with an LLM, which generates interactive animated objects using Anime.js that can be rendered and manipulated on the canvas

## What's here

Generated by LiveForge using the **HTML + CSS + JS** recipe (AI-native).

Files in this directory:

- `VISION.md` — what this app is and why
- `README.md` — this file
- `index.html` — Document shell. Must reference styles.css via <link rel="stylesheet" href="styles.css"> and app.js via <script src="app.js" defer>. Single <script> tag only.
- `styles.css` — All global styles. Define theme tokens at :root and reference them via var(--token).
- `app.js` — SINGLE-FILE entry point containing ALL application logic — DOM wiring, view rendering, state, components, helpers. No imports, no exports, no module syntax. Use plain function declarations and IIFEs to organize code.

## How to run

Open `index.html` in any modern browser, or serve the directory with any static file server:

```bash
python3 -m http.server 8000
# then visit http://localhost:8000
```

No build step. No dependencies. The CSS variables in `styles.css` are the theme — change them to retheme the app.

## How this was built

LiveForge generated this app through a multi-stage pipeline:

1. **Clarifier** — read your brief and proposed a structured spec
2. **Scaffolder** — wrote VISION.md and README.md from the spec
3. **Planner** — designed the file-by-file generation plan
4. **Generator** — wrote each source file
5. **Verifier** — ran static / render / console / interaction / spec-conformance checks
6. **Fixer** *(if needed)* — repaired anything the verifier flagged
7. **Committer** — saved this version

Open VISION.md to see what the app is meant to do — it's the source of truth for refinements.

---

_Generated by LiveForge._
